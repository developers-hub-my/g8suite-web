---
import SectionHeader from '~/components/ui/SectionHeader.astro';

const principles = [
  {
    title: 'Sovereignty',
    description: 'Every product runs inside your perimeter. No phone-home telemetry, no external dependencies, no shared multi-tenant surfaces.',
  },
  {
    title: 'Shared Identity',
    description: 'One identity model flows through every product. No federated stitching, no per-tool user directories, no reconciling one vendor’s directory against another.',
  },
  {
    title: 'Shared Audit',
    description: 'Every action across every product writes to one unified audit trail. One source of evidence for every regulator.',
  },
  {
    title: 'Shared Workflow',
    description: 'Approvals, escalations, and orchestrations are first-class across the suite. Cross-product processes are designed in, not custom integration work.',
  },
  {
    title: 'Event-Driven Integration',
    description: 'Products communicate over a typed event bus, not point-to-point integrations. Adding a product extends the suite without breaking it.',
  },
];
---

<section id="architecture" class="section bg-soft">
  <div class="container-page">
    <SectionHeader
      eyebrow="Architectural principles"
      title="Why the suite holds together."
      subtitle="Five principles every product observes. These make g8suite a true suite — not six products sharing a logo."
    />

    <ol class="mt-14 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">
      {principles.map((p, i) => {
        const padded = String(i + 1).padStart(2, '0');
        return (
          <li class="flex h-full flex-col rounded-xl border border-line bg-card p-6 transition-all hover:-translate-y-0.5 hover:border-brass hover:shadow-elevated">
            <div class="flex items-baseline justify-between">
              <span class="font-mono text-3xl font-bold leading-none text-brass">{padded}</span>
              <span class="h-px flex-1 ml-3 bg-line"></span>
            </div>
            <h3 class="mt-5 text-base font-semibold text-body">{p.title}</h3>
            <p class="mt-2 text-sm leading-relaxed text-muted">{p.description}</p>
          </li>
        );
      })}
    </ol>

    <div class="mt-10 rounded-r-xl border-l-2 border-green bg-green/5 p-7 lg:p-8">
      <p class="flex items-start gap-3 text-base leading-relaxed text-body lg:text-lg">
        <svg class="mt-1 h-5 w-5 flex-shrink-0 text-green" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M4 10l4 4 8-8" stroke-linecap="round" stroke-linejoin="round" />
        </svg>
        <span>
          These five principles are non-negotiable. Every product that joins the suite must observe all five — or it doesn't join the suite.
        </span>
      </p>
    </div>
  </div>
</section>
