[build]
  command = "npm run build"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    # 'unsafe-inline' for script-src is needed because Astro inlines small bundled
    # <script type="module"> blocks directly into the HTML (mobile menu + scroll state).
    # static.cloudflareinsights.com + cloudflareinsights.com allow Cloudflare Web
    # Analytics (auto-injected when the site is fronted by Cloudflare).
    # googletagmanager.com + google-analytics.com allow Google Analytics (gtag.js),
    # loaded only when PUBLIC_GA_MEASUREMENT_ID is set at build time.
    # All other script content is from our own bundled build — no user input on the page.
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://static.cloudflareinsights.com https://www.googletagmanager.com; connect-src 'self' https://cloudflareinsights.com https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https://www.google-analytics.com https://www.googletagmanager.com; font-src 'self' data:;"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
