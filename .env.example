# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX).
# PUBLIC_ prefix is required — Astro only exposes PUBLIC_* vars to the client build.
# Leave unset locally to keep GA out of dev builds; set it in Netlify env vars for production.
PUBLIC_GA_MEASUREMENT_ID=
